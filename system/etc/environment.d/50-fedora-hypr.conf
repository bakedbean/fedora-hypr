FH_PATH=/usr/share/fedora-hypr
# PATH (~/.local/bin) and DOCKER_HOST live in /usr/share/uwsm/env: environment.d cannot
# expand $HOME or ${XDG_RUNTIME_DIR} (the manager's own variables are not visible to the
# generator), so a value like unix://${XDG_RUNTIME_DIR}/… reaches the session verbatim.
