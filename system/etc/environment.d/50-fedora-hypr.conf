FH_PATH=/usr/share/fedora-hypr
# waybar-docker (and docker CLI via podman-docker) talk to the rootless podman socket,
# started by default/hypr/autostart.conf. environment.d expands $VARS (not %t specifiers).
DOCKER_HOST=unix://${XDG_RUNTIME_DIR}/podman/podman.sock
