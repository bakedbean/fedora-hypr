# This directory holds active runtime config toggles (managed by fh-toggle-enabled).
# Do not remove this file — the directory always needs at least one file for
# the wildcard `source` in hyprland.conf to resolve.
