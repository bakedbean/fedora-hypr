# swayosd only reads ~/.config/swayosd/config.toml (no include directive,
# and no /usr/share default is consulted) — this file is yours to edit.
[server]
show_percentage = true
max_volume = 100
style = "~/.config/swayosd/style.css"
