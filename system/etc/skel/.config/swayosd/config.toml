# swayosd's config.toml has no include directive, so this is a copy of
# /usr/share/fedora-hypr/default/swayosd/config.toml (not a reference to it).
# To customise, edit this file directly.
[server]
show_percentage = true
max_volume = 100
style = "/usr/share/fedora-hypr/default/swayosd/style.css"
