# Let Hyprland handle the power key (bindings/utilities.conf: XF86PowerOff -> fh-menu system)
# instead of logind powering off immediately. Holding the button still forces off in firmware.
[Login]
HandlePowerKey=ignore
